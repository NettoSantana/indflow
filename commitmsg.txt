fix(machine/config): recarregar config_v2 do SQLite apos deploy

- Aplica config_v2 persistida (machine_config.config_json) na memoria em:
  /machine/status, /machine/update e /api/producao/historico
- Garante que a tela de configuracao e calculos usem a config salva mesmo apos restart/deploy
