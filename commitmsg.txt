fix(admin): endpoint para limpar producao_horaria com cliente_id errado (token)

- Adiciona POST /admin/db-fix-producao-horaria protegido por ADMIN_RESET_TOKEN/INDFLOW_ADMIN_TOKEN
- Remove linhas por data_ref/machine_id/cliente_id incorreto e retorna contagens before/after
