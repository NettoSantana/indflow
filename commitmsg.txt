fix(historico): modal por hora mostrar somente estado (verde/verm/cinza) usando /machine/status

- Modal por hora agora exibe apenas PRODUZINDO / PARADO / FORA DO HORARIO
- Hora atual usa estado ao vivo do /machine/status (mesmo do card do Dashboard)
- Horas passadas usam segments quando existir
- Remove dependencia de producao/meta para cor
