fix(front-op): OP ativa nao atualizava modal (refresh travado)

- fetchOpGet hidrata __dia_ref e ativa a partir de status
- startOpModalLiveRefresh usa status ATIVA e dia via started_at/__dia_ref (remove chamada isHoje inexistente)
- apos ativar/encerrar reabre modal em view para refresh funcionar
