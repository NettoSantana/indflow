feat(historico): adicionar endpoint /api/producao/detalhe-dia para modal com tabela horaria e timeline RUN/STOP/NP

- Retorna hours[0..23] com meta, produzido, refugo e segments (RUN/STOP/NP) por hora
- Usa oee.no_count_stop_sec do machine_config (fallback 120)
- Marca NP quando meta da hora = 0
