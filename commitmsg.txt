fix(historico): barra por status (RUN/STOP/NP), sem depender de producao

- Remove fallback que marcava RUN pela producao quando machine_state_event esta vazio
- Para hora atual, usa machine_state (run/status/status_ui) para definir RUN/STOP
- Para horas passadas sem eventos, nao inventa historico (assume STOP)
