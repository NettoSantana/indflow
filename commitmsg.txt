fix(machine): fechamento diario multi-tenant grava cliente_id no historico

- seta m['cliente_id'] no /machine/update antes do reset diario
- evita fechamento com data vazia no primeiro pacote
- marca producao_diaria com cliente_id no fechamento (historico volta a aparecer)
