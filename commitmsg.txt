fix(machine): /machine/config volta a salvar configs (resolve cliente_id)

- corrige NameError em configurar_maquina(): cliente_id agora vem de _get_cliente_id_for_request()
- mantém lógica de histórico multi-tenant
