revert(front): voltar fluxo simples no card OP (sem chips, com botao Abrir)

- Remove fila em chips do card OP
- Restaura botao Abrir (btn-op-manage) para abrir o modal
- Remove chamadas automaticas de carregarOpFila/setInterval
