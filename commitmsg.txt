feat(indflow-dev): recarga robusta do histórico por hora (SQLite)

- recarrega producao_por_hora quando muda data_ref (virada de turno/dia) ou muda len(horas_turno)
- não marca _ph_loaded=True quando falha (tenta de novo no próximo ciclo)
- mantém persistência por hora via upsert em producao_horaria
