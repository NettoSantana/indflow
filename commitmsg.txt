fix(historico): exibir tempo prod/parado a partir de *_sec (fallback *_min)

- front passa a ler tempo_produzindo_sec e tempo_parado_sec do detalhe-dia
- converte segundos para minutos na exibicao
- mantem compatibilidade com campos em minutos se existirem
