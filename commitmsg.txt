fix(historico): corrigir escopo JS que quebrava modal (buildSegmentsForHour)

Corrige bloco de funcoes (normalizeStateFromStatus/getStatusTsMs/stateToCss/buildSegmentsForHour) para ficarem no escopo correto

Remove ReferenceError e faz o modal "Detalhe do dia" voltar a renderizar o rastro por estado
