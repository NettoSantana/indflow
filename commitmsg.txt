fix(producao): recode sem remover codigo; corrigir salvar OP e totais por OP

- DB_PATH passa a usar INDFLOW_DB_PATH (/data/indflow.db)
- sqlite connect com check_same_thread=False
- corrige placeholders do INSERT de ordens_producao (18 colunas)
- remove chave duplicada op_conv_m_por_pcs no payload
- op_mem guarda bobinas e op_conv_m_por_pcs
- historico passa a receber op_metros/op_pcs/op_conv_m_por_pcs nas OPs
