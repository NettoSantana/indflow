feat(producao-card): trocar AUTO->PRODUZINDO, MANUAL->PARADA e exibir tempo parado no card

- Badge verde agora mostra "PRODUZINDO" quando status=AUTO
- Quando não AUTO, mostra "PARADA" e linha "XX min parados" abaixo do badge
- Tempo parado é calculado no navegador e persistido por máquina via localStorage
