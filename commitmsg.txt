fix(front): restaurar botao Abrir no card OP e remover chips sem deletar blocos grandes

- Remove bloco HTML dos chips da fila no card OP
- Restaura botao Abrir (btn-op-manage) no card OP
- Mantem CSS/JS existentes (alteracao minima, sem corte de linhas)
