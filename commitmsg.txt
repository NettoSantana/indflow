fix(db): criar tabelas de bobinas (eventos e pendencia) no init_db

- Adiciona ordens_producao_bobina_eventos com UNIQUE(op_id, seq)
- Adiciona ordens_producao_bobina_pendencia para travar clique duplo e sincronizar inicio no primeiro machine/update
- Cria indexes e faz migracao leve via add_column_if_missing
