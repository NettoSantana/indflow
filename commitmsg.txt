fix(db): migracao leve para adicionar cliente_id sem quebrar db existente

- adiciona ALTER TABLE para colunas faltantes (cliente_id/created_at)
- cria indices somente apos garantir colunas
- evita erro no Railway: no such column cliente_id
