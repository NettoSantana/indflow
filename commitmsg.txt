fix(producao/op): activated_at persistido + op/get retorna bobinas_detail com eventos

- Adiciona coluna activated_at em ordens_producao (create + migration)
- /producao/op/get passa a retornar activated_at
- /producao/op/get monta bobinas_detail com started_at/ended_at via ordens_producao_bobina_eventos
- Aplica fallback de comprimento e calcula pcs_total/metro_consumido quando possivel (principalmente com OP ATIVA)
