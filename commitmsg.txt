fix(historico): corrigir datetime naive/aware no detalhe-dia

- Converte intervalos RUN de machine_state_event para datetimes naive (Bahia)
- Mantem calculo de day_start_ms via aware apenas para timestamp correto
- Remove erro 500: can't compare offset-naive and offset-aware datetimes
