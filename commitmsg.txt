fix(historico): usar producao_exibicao_24 do machine_state como fallback no detalhe-dia

- Evita Prod=0 e barras vermelhas quando a persistencia horaria ainda nao gravou
- Aplica apenas para o dia atual (HOJE) e somente se existir producao_exibicao_24
