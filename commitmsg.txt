fix(machine): salvar campo run vindo do ESP e expor no /machine/status

- Persiste m["run"] no /machine/update (payload do ESP)
- Garante run no JSON de /machine/status (evita undefined)
- Destrava contagem de np_minutos fora do turno
