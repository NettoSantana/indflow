fix(historico): barra por status na hora atual e nao inferir STOP sem eventos

- Remove fallback por producao quando nao houver machine_state_event
- Hora atual usa status atual (run/status_ui) para RUN/STOP
- Horas sem eventos viram IDLE (cinza no front)
