chore: adicionar logging minimo no server (startup + requests)

- Loga CWD e INDFLOW_DB_PATH no startup
- Loga metodo/path/status/ip em cada request (after_request)
- Envolve init_db com logs e exception
