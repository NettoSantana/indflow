fix(historico): evitar produzido acumulado por hora (delta via baseline_esp/esp_last)

- No detalhe-dia do dia atual, converte acumulado do ESP em delta por hora quando colunas existem
- Hora corrente usa esp_agora - baseline_da_hora (alinha com card)
- Zera horas futuras para nao herdar acumulado
