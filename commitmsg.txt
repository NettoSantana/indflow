fix: trava de persistencia no status para evitar producao dobrada

- /machine/status nao chama atualizar_producao_hora (evita gravar/persistir por polling)
- Persistencia de producao fica somente no /machine/update (ESP)
