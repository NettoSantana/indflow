fix(config_maquina): remover chips da OP e restaurar botao Abrir

- Header (PATH/LAST_RECODE/MOTIVO) em comentario HTML para nao renderizar
- Card OP volta ao fluxo simples com botao Abrir (btn-op-manage)
- Removida renderizacao de fila/chips no card OP
- Removidas chamadas JS de carregarOpFila() e interval de fila
