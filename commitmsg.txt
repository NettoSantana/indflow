feat: dashboard usa unidades da configuração (unidade_1 e unidade_2)

- remove hardcoded UNI/ML do card
- unidade_1 (obrigatória) aparece na linha de cima
- unidade_2 (opcional) aparece na linha de baixo e some quando vazia
- valores seguem a unidade (pcs -> base; m -> campos *_ml; m2 -> base)
