feat(machine): auto-parada por falta de contagem (backend)

- cria machine_count_state (last_esp_abs, last_count_ms)
- /machine/update atualiza last_count_ms quando esp_absoluto incrementa
- /machine/status: mesmo em AUTO, se sem contagem por INDFLOW_NO_COUNT_STOP_SEC => status_ui=PARADA
