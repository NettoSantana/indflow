fix(producao): criar API /api/producao/historico e endpoint de salvar diaria no SQLite

- inicializa SQLite via init_db ao carregar routes
- adiciona pagina /producao/historico e rota JSON /api/producao/historico
- adiciona /api/producao/salvar_diaria para persistir produzido/meta
- adapta campos para o front (pecas_boas=produzido, refugo_total=0)
