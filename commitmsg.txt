fix(historico): usar producao_por_hora no detalhe-dia para Prod do historico

- No /api/producao/detalhe-dia, Prod por hora prioriza machine_state.producao_por_hora (mapeada por horas_turno)
- Fallback para producao_exibicao_24 apenas se nao houver mapeamento
- Evita Prod 0 no historico quando o card ja mostra producao na hora, sem herdar na virada
