fix(machine_routes): meta do dia correta no /machine/status

- Calcula meta_dia como soma das metas de todos os turnos (config_v2.shifts)
- meta_turno passa a representar a meta do dia (para card/producao_diaria)
- meta_por_hora usa apenas meta_turno_ativo (turno atual), evitando multiplicacoes indevidas
