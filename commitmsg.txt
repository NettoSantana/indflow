fix: endpoint para purge total das tabelas operacionais (producao/estado)

- adiciona POST /admin/purge-production (apaga definitivamente dados operacionais)
- preserva cadastros (clientes, usuarios, devices, machine_config etc)
- opcional: filtra por machine_id quando a tabela tiver coluna machine_id
