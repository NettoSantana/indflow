fix(producao_hora): fora do turno usar np_producao_hora (zera por hora) e evitar salto

- /machine/status: no fallback fora do turno, usar np_producao_hora em vez de np_producao
- evita pulo/repetição na tabela por hora e garante zerar na virada da hora
