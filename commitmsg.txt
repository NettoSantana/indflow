fix: remap meta_por_hora do turno para tabela fixa 24h

- tabela permanece fixa 00–24 (24 linhas)
- meta_por_hora agora é deslocada pelo inicio do turno (ex.: 08:00 -> metas aparecem 08–16)
- produzido hora mantém Regra A (não exibe horas futuras) e usa producao_por_hora
