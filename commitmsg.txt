feat(op): lógica de ativação de OP e recarga segura do histórico

- Ajuste no historico.html para suportar fluxo Ativar OP → Encerrar OP
- Preparação da tela para cálculo de PCS da OP baseado no contador do ESP
- Estrutura para cálculo de metro linear consumido (pcs × conversão)
- Estrutura para cálculo de tempo de consumo a partir da hora inicial
- Campos manuais de classificação: costuras, refugo e retrabalho
- Removida chamada incorreta de loadHistorico()
- Mantida recarga segura do histórico após salvar OP

Arquivo alterado:
templates/historico.html
