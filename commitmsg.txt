fix(machine_routes): virada de hora robusta contra atraso de timestamp do ESP

- Se ts_ms do ESP tiver drift alto vs servidor, usar relogio do servidor (America/Bahia) para bucket de hora
- Evita hora_idx atrasar e hora nova herdar producao por timestamp oscilando
- Mantem uso normal do ts_ms do ESP quando dentro da janela aceitavel
