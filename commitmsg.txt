feat(dashboard): botao + cria novo equipamento (card) via modal e redireciona para configuracao

- modal pede ID do equipamento (ex: maquina02)
- lista de equipamentos salva em localStorage (modo manual/prototipo)
- dashboard renderiza cards dinamicamente e consulta /machine/status por machine_id
