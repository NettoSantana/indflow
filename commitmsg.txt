fix(op): fila sem limite e posicao automatica no /producao/op/iniciar

- Remove limite 1..5 e erro de fila cheia
- Trata posicao=0 como nao informado (front manda 0 quando vazio)
- Auto-atribui posicao como (max ocupadas + 1), mantendo unicidade
