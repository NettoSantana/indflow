fix(indflow-dev): restaurar ultimo_dia/baseline/esp_last do SQLite no boot

- get_machine carrega baseline_diario (dia_ref, baseline_esp, esp_last) do DB
- evita ancorar baseline em 0 quando /machine/status roda antes do /machine/update após deploy
- mantém ultimo_dia como string YYYY-MM-DD
