fix(indflow-dev): DB sqlite preparado para volume (path absoluto via INDFLOW_DB_PATH)

- evita DB relativo (perda de dados em container efêmero)
- usa /data/indflow.db quando existir volume
- cria pasta do DB automaticamente
- sqlite com timeout e check_same_thread=False (waitress)
