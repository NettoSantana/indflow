fix(refugo_repo): corrigir salvamento legacy sem quebrar multi-tenant

- remove ON CONFLICT do caminho legacy (cliente_id IS NULL) por incompatibilidade com indice parcial
- aplica UPDATE + INSERT quando necessario, mantendo resto do arquivo intacto
