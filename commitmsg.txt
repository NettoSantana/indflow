fix(machine): acumular tempo não programado com heartbeat (segundos -> minutos)

- Evita round(dt/60) que zerava np_minutos com heartbeat de 5s
- Adiciona acumulador m["_np_secs"] e converte para minutos quando >= 60s
- Usa run=1 como sinal principal de atividade fora do turno
