fix: corrigir auth do admin purge (check_admin_auth + get_db_path)

- Adiciona get_db_path() para padronizar INDFLOW_DB_PATH (/data/indflow.db)
- Implementa _check_admin_auth() usando token (INDFLOW_ADMIN_TOKEN/ADMIN_RESET_TOKEN)
- Remove NameError no POST /admin/purge-production (Railway DEV)
