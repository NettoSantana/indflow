fix: corrigir SyntaxError no machine_routes e criar reset por data

- Corrige bloco try/except que estava quebrando o deploy no Railway
- Adiciona endpoint POST /admin/reset-date para zerar producao por dia (YYYY-MM-DD)
- Mantem /admin/reset-manual e torna reset mais robusto contra variações de schema
