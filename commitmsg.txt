fix(producao): persistir producao por hora no SQLite (baseline_hora + producao_horaria)

- Grava baseline por hora no primeiro evento da hora
- Atualiza snapshot de producao_horaria a cada update do ESP e no polling do status
- Mantem horas anteriores congeladas e nao perde historico apos deploy
