feat(producao-card): exibir PRODUZINDO/PARADA e 'XX min parados' no dashboard

- Badge agora usa status_ui (fallback: status AUTO/...)
- Quando PARADA, mostra parado_min como 'XX min parados' abaixo do badge
- Implementado somente em static/dashboard.ui.js (sem mexer em percentuais)
