fix(ui): destravar edicao de horarios em turnos/pausas na config_maquina_for

- Evita recarregar config do servidor durante edicao (configDirty)
- Atualiza tabela 24h via cache local (atualizarTabelaLocal)
- Limpa dirty apos salvar e recarrega do servidor
