feat(multitenant): amarrar ESP ao cliente via X-API-Key e proteger device por tenant

- /machine/update resolve cliente pelo X-API-Key e grava device com cliente_id
- bloqueia MAC que já pertence a outro cliente (403)
- resolve vínculo device->machine filtrando por cliente_id
- baseline inicial tenta usar cliente_id quando coluna existir (migração leve)
