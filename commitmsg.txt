fix(producao): historico com contagem ao vivo + timezone Bahia (-03:00) + OP ativa com totais

- started_at/ended_at em ISO com timezone America/Bahia
- historico soma producao do dia detectando automaticamente tabela/colunas no SQLite
- OP ativa retorna op_metros/op_pcs calculados (bobinas + conversao, floor) no historico e no /op/status
- adiciona import os (corrige _get_tz)
