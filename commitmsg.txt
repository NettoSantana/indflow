fix(detalhe-dia): produzido por hora via SUM(delta) em producao_evento (TZ America/Bahia)

- hours[].produzido passa a ser calculado por janela de hora local usando ts_ms
- remove sobrescrita por machine_state (producao_exibicao_24 / horas_turno) que zerava horas
- producao_horaria fica como fallback quando nao houver eventos
