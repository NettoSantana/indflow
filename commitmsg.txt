fix(producao_horaria_repo): resolver cliente_id consistente e ignorar scoped cid

- Antes: cliente_id vinha do machine_id no formato cliente_id::machine_id, podendo partir dados entre clientes
- Agora: resolve cliente_id consultando o SQLite por machine_id e usa scoped apenas como fallback
- Aplica a mesma normalizacao em upsert e leituras (baseline/load) para manter historico consistente
