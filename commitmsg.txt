fix(ui): corrigir modal do dia no historico (markup + fetch + clique OP)

- Remove modal do dia que estava inserido dentro do modal de OP (markup quebrado)
- Corrige comentario HTML e estrutura do DOM
- Adiciona fallback de endpoints no fetch do detalhe do dia
- Garante stopPropagation no clique da linha OP
