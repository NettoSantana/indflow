feat(indflow-dev): persistência de produção por hora no SQLite + recarga após restart

- cria tabela producao_horaria (por hora do turno)
- grava baseline/produção/meta/% por hora (upsert)
- preenche m['producao_por_hora'] para a tabela manter horas anteriores
- continua zerando o produzido da hora atual ao virar a hora (baseline_hora)
