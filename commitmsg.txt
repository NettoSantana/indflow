feat(historico): mostrar informacoes por hora (meta, produzido, tempos e paradas) a partir do JSON

- adiciona helpers pickFirstNumber e fmtMinutes
- exibe acima da barra por hora: Meta, Prod, Tempo Prod, Tempo Parado, Paradas (quando existirem no JSON)
- nao calcula nada no frontend; apenas renderiza os campos recebidos
