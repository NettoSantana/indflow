fix(historico): ler producao_horaria do machine_id correto (scoped/unscoped) no detalhe-dia

- _fetch_horaria() sempre retorna 24 chaves mesmo sem linhas; isso fazia o detalhe-dia ficar zerado
- _fetch_horaria_multi agora só aceita o resultado quando existe algum dado != 0 (produzido/meta/refugo/baseline/esp)
- Resolve persistencia da hora anterior aparecendo zerada no detalhe-dia por mismatch de machine_id
