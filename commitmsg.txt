fix(ui): tabela 24h usa producao_exibicao_24 (opção 1) como fonte única

- Remove decisão duplicada (turno vs NP) para o produzido/hora
- Mantém destaque visual do turno e refugo editável
- Fallback controlado para np_por_hora_24 quando backend ainda não enviar o campo
