fix(producao/op): encerrar calcula esp_atual final com prioridade e persiste op_metros float

- /producao/op/encerrar e /producao/op/encerrar-by-id: esp_atual = JSON > snapshot DB > safe fallback
- op_pcs = esp_atual - baseline_pcs
- op_metros = op_pcs * conv (float) e _close_op_row_v2 não trunca para int
- fechamento do evento de bobina usa esp_atual final correto
