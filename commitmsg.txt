fix(historico): detalhe-dia usar producao_exibicao_24 no dia atual sem zerar tudo

- Remove normalizacao por esp_last/baseline que estava zerando produzido
- Para hoje, hours[].produzido vem do array producao_exibicao_24 (24h), evitando herdar hora anterior
