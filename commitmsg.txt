fix(np): baseline por hora ancorado no esp_absoluto (evita explosão np_hour_baseline pequeno)

- np_hour_baseline passa a ser o esp_absoluto ao virar a hora (fora do turno)
- np_producao_hora vira esp_absoluto - np_hour_baseline
- mantém np_producao diário como soma de deltas
