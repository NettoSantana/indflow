fix(historico): calcular produzido por hora via eventos reais (producao_evento)

- Detalhe-dia passa a sobrescrever produzido por hora com delta real dentro do intervalo HH:00-HH+1:00
- Evita hora zerada (ex 07-08) quando dashboard mostra producao na hora
- Mantem compatibilidade: se nao houver eventos, usa producao_horaria como fallback
