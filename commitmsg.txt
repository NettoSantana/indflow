fix(a11y): associar labels aos campos no config_maquina_for

Corrige avisos "No label associated with a form field" no DevTools/Lighthouse

Adiciona for/id nos campos fixos (ideal, rampa, inatividade, conversao, OP)

Ajusta seções (labels de título viram div) para evitar label sem input

No JS: adiciona id + htmlFor nos inputs de turnos; aria-label em pausas/bobinas/refugo
