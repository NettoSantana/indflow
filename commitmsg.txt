feat: refugo deixa de usar localStorage e passa a persistir no SQLite

- carrega refugo_por_hora (24) via /machine/status
- salva refugo via POST /machine/refugo (dia_ref + hora_dia)
- mantém regra: só horas passadas editáveis
- produzido hora exibido como (produzido - refugo), clamp >= 0
