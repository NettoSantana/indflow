fix(machine_update): iniciar bobina seq seguinte no primeiro update apos troca

- /machine/update agora aplica pendencia de troca de bobina:
  ao detectar ultimo evento fechado e existir bobina seguinte cadastrada,
  cria/atualiza ordens_producao_bobina_eventos seq N+1 com started_at e start_abs_pcs
- Mantem regra: somente 1 bobina consome por vez
