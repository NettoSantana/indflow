fix(historico): usar segmentos RUN/STOP/NP na barra e atualizar modal em tempo real

Modal detalhe-dia passa a renderizar barra por segmentos (verde/vermelho/cinza) quando backend envia hours[].segments

Atualiza automaticamente a cada 5s enquanto o modal estiver aberto para refletir parada/produção do momento

Mantém fallback antigo quando não houver segments
