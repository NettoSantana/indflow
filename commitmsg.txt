fix(machine_calc): corrigir imports ausentes (time/timedelta) que zeravam o backend

- Adiciona imports de datetime/timedelta/time e ZoneInfo no topo do modulo
- Mantem toda a logica de persistencia existente, sem mexer em historico/front
