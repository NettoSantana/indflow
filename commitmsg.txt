feat(producao-card): usar status_ui/parado_min do backend e padronizar PRODUZINDO/PARADA

- Card passa a mostrar PRODUZINDO/PARADA (nunca MANUAL)
- Exibe 'XX min parados' usando parado_min do /machine/status
- Mantém fallback compatível com backend antigo (AUTO/MANUAL)
