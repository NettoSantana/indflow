feat(security): historico diario multi-tenant e baseline index seguro

- /api/producao/historico exige tenant (api key ou sessao) e filtra por cliente_id
- compatibilidade com machine_id legado no formato cliente_id::maquina
- remove criacao do UNIQUE antigo ux_baseline_diario e cria indices (tenant + legado parcial)
- protege /producao/historico com login_required
