feat(machine): suportar configuracao v2 (turnos + pausas planejadas) no POST /machine/config

- Aceita payload v2 com shifts/breaks/active_days/oee/units
- Converte formato legado (1 turno) para v2 automaticamente
- Persiste config_json na tabela machine_config (SQLite)
- Mantem compatibilidade atual atualizando memoria e chamando upsert_machine_config
