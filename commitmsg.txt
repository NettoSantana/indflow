fix(machine_routes): corrigir SyntaxError e expor np_por_hora_24 no /machine/status

- adiciona helpers _ensure_np_horaria_table e _load_np_por_hora_24
- preenche m['np_por_hora_24'] dentro de machine_status (indentação correta)
- evita except solto / bloco fora de função
