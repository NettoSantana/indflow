style(front): fila de OP no topo (chips) e remover card do grid

- Remove o card "Fila de OP" do grid
- Renderiza 5 slots (1..5) no topo, lado a lado, na linha do titulo
- Mantem consumo de GET /machine/op/fila e refresh automatico
