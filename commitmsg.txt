fix(producao/op): persistir pcs/metros corretos no encerrar (esp seguro + metros=pcs*conv)

- encerrar e encerrar-by-id: usa esp_atual do request (se vier) + snapshot do banco + fallback safe
- op_pcs = esp_atual - baseline_pcs
- op_metros = op_pcs * conv (nao soma comprimento da bobina)
- _close_op_row_v2 salva op_metros como float (sem truncar)
