fix(historico): alinhar produzido por hora no detalhe-dia com o card (delta ESP)

- Remove sobrescrita de produzido por hora via contagem de eventos (producao_evento) que gerava valores baixos (ex 72)
- Produzido por hora passa a usar delta do contador do ESP (mesma logica do dashboard)
- Corrige indentacao que deixava comentario fora do bloco try e podia quebrar import/execucao
