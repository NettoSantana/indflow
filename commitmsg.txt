feat: persistência de refugo por hora no SQLite

- cria tabela refugo_horaria (machine_id + dia_ref + hora_dia)
- adiciona endpoint POST /machine/refugo com validação (apenas horas passadas)
- status passa a devolver refugo_por_hora (24) e producao_hora_liquida
