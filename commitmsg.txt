fix(op): bloquear OS duplicada por maquina

- Valida machine_id + OS em /producao/op/iniciar
- Valida troca de OS em /producao/op/editar (ativo e historico)
- Tenta criar UNIQUE INDEX (machine_id, os) no SQLite (fallback para validacao no endpoint)
