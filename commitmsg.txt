feat(multitenant): baseline_repo com cliente_id nativo + migracao segura

- adiciona cliente_id na tabela baseline_diario (ALTER TABLE se faltar)
- cria unique index multi-tenant (cliente_id,machine_id,dia_ref)
- cria unique index legado (machine_id,dia_ref) apenas quando cliente_id IS NULL
- suporta compatibilidade com machine_id no formato cliente::maquina
