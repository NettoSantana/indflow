fix(producao): rota /config renderiza template do formulario

- GET /producao/config/<machine_id> agora renderiza config_maquina_for.html
- Evita abrir o HUB antigo (config_maquina.html global) por engano
