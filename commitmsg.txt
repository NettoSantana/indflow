fix(np): status ler NP com tenant correto (cliente_id scoping)

- Salva cliente_id no estado da máquina durante /machine/update (via API key)
- /machine/status resolve tenant via API key/sessão e fallback no estado
- Evita np_por_hora_24 zerado por mismatch entre 'cliente::machine' e 'machine'
