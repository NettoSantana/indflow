debug(np): expor falha de persistencia do NP no status

- Adiciona _np_repo_ok, _np_persist_stage, _np_persist_error no estado m
- Captura excecoes de load/persist sem quebrar update
- Mantem OPCAO A de reancoragem ao entrar em fora-do-turno
- Sem remover linhas; apenas adicao e ajuste pontual em machine_service.py
