fix(producao_horaria): persistir producao por hora no /machine/update

- Faz upsert em producao_horaria para a hora atual (hora_idx) a cada pacote do ESP
- Mantem card zerando a cada virada de hora, mas historico fica gravado e sobrevive a deploy/restart
- Usa timestamp do ultimo pacote do ESP quando disponivel para evitar drift do server
