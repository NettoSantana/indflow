fix(historico): barra do detalhe-dia baseada em segmentos do backend

- Prioriza h.segments (machine_state_event) para desenhar RUN/STOP/NP
- Evita depender de localStorage/visita previa (aba anonima nao fica zerada)
- Mantem fallback antigo caso backend nao envie segmentos
