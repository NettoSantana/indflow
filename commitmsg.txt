fix(producao-card): remover conflito AUTO/MANUAL e padronizar PRODUZINDO/PARADA no update loop

- dashboard.update.js agora usa status_ui (fallback status) no badge
- exibe 'XX min parados' com parado_min no elemento stopline-*
- evita alternancia com AUTO/MANUAL
