fix(front/historico): live refresh e validacoes no modal OP

- Modal OP em modo view faz refresh a cada 5s via /op/get quando OP ativa e dia atual
- Valida duplicidade de OS usando cache do historico (frontend)
- Valida consistencia por bobina: costuras+refugo+retrabalho <= total pcs (quando total pcs > 0)
