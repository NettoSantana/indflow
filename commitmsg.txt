fix(historico): hora atual do detalhe-dia usa producao_hora em memoria (igual card)

- Para o dia atual, a hora corrente (HH:00-HH+1) usa machine_state.producao_hora
- Remove sobrescrita de produzido por contagem de eventos (producao_evento) que gerava valores baixos
- Garante horas futuras zeradas no detalhe-dia
