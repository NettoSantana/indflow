fix(producao/routes): bobina inicia na ativacao e op_get inclui eventos; encerrar salva metros por pcs*conv

- Nao cria evento de bobina no criar OP (FILA); evento passa a ser ancorado na ativacao
- /op/get retorna bobinas_eventos e faz fallback de bobinas_detail via eventos com started_at/ended_at
- Encerramento grava op_metros = op_pcs * op_conv para evitar historico zerando
