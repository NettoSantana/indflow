fix(np): calcular e persistir hora extra por hora (zera na virada)

- adiciona np_hour_ref/np_hour_baseline em nao_programado_diario
- calcula np_producao_hora = np_producao - baseline da hora
- reseta baseline na virada da hora para evitar 'pulos' na UI
