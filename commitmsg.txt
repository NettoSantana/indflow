fix: reset-date tambem limpar OPs mesmo sem coluna de data padrao

- normaliza machine_id no reset por data
- ordens_producao: se nao houver coluna de data padrao, usa created_at (se existir) ou remove por machine_id
