fix(frontend): corrige sintaxe JS no config_maquina e mantém somente Nova OP

- Corrige chaves soltas/blocos duplicados e restos de try/catch
- Mantém removido o box OP ativa no topo
- Mantém removido o encerrar OP; modal só inicia nova OP (/producao/op/iniciar)
