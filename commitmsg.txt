fix(detalhe-dia): meta por hora deve vir do estado da maquina (meta_por_hora + turno_inicio)

- Converte meta_por_hora do turno para vetor 24h do relogio
- Aplica em hours[].meta no endpoint detalhe-dia
- Mantem produzido/refugo do banco
- Fallback: se nao houver meta_por_hora/turno_inicio, usa meta do banco
