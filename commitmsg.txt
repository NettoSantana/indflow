feat(multitenant): isolar calculos por cliente usando namespace cliente_id::machine_id

- adiciona helper _scoped_machine_id
- baseline/producao_horaria/producao_diaria passam a usar machine_id escopado
- mantem contratos externos (machine_id visivel nao muda)
