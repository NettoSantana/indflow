fix(indflow-dev): adicionar carregar_baseline_turno no machine_calc

- corrige ImportError no boot (waitress) ao importar em machine_routes
- baseline_turno cria sob demanda (CREATE TABLE IF NOT EXISTS)
