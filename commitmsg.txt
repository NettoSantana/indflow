feat(auth): adicionar login web por sessão e proteger rotas do admin

- cria /admin/login (form inline) e /admin/logout
- adiciona decorator login_required e protege /admin/
- /admin/bootstrap fica livre só no primeiro uso; depois exige admin logado
- mantém token do ESP (API key) intacto
