fix(machine_routes): persistir transicoes RUN/STOP no /machine/update

- Grava eventos de estado no momento do payload do ESP (usa ts_ms quando existir)
- Elimina dependencia do polling do /machine/status para formar a timeline
- Mantem regra RUN=AUTO+run=1 e STOP caso contrario (considera no_count_stop_sec)
