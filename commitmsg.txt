fix(np): persistir hora extra por delta/hora em nao_programado_horaria

- Adiciona UPSERT somando delta por (machine_id, data_ref, hora_dia)
- Ativa modo NP quando ultima_hora=None (fora do turno)
- Respeita dia operacional (data_ref) e evita usar acumulado diário
- Chama persistência no POST /machine/update após atualizar_producao_hora
