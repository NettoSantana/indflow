fix(front): historico busca endpoint correto com prefixo /producao para listar OPs como subitem

- altera fetch de /api/producao/historico para /producao/api/producao/historico
- permite renderizar r.ops abaixo de cada dia
