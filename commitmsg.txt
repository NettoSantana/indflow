fix(producao_horaria_repo): fixar cliente_id e impedir mistura entre tenants

- Ignora cliente_id vindo de machine_id scoped
- Resolve cliente_id por tabelas de config/vinculo no SQLite
- Fallback seguro pelo cliente_id mais recente em producao_horaria
