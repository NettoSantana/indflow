fix(op-get): incluir baseline_pcs e calcular op_pcs/op_metros ao vivo quando ATIVA

- /producao/op/get passa a devolver baseline_pcs
- quando status=ATIVA: calcula op_pcs = esp_atual - baseline_pcs e op_metros = op_pcs * conv_m_por_pcs
- inclui esp_atual no payload para conferência
