fix(np): persistir NP no update e garantir scoping por cliente_id

- /machine/update salva m['cliente_id']=cliente_id e chama processar_nao_programado()
- /machine/config remove uso de variavel cliente_id inexistente; tenta obter tenant do request
- Destrava np_por_hora_24 e producao_exibicao_24 fora do turno (OPÇÃO 1)
