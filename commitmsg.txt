chore: criar endpoint admin /admin/db-check para inspecionar SQLite no Railway

- Adiciona /admin/db-check (JSON) protegido por token (INDFLOW_ADMIN_TOKEN ou ADMIN_RESET_TOKEN)
- Lista tabelas e contagens principais
- Retorna amostras de producao_horaria e producao_diaria para maquina02
