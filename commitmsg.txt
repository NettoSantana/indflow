fix(frontend): remove OP ativa no topo e remove encerrar OP na tela de config

- Remove box "OP ativa" no canto superior
- Remove botao/acao de encerrar OP no modal
- Modal passa a apenas iniciar nova OP via /producao/op/iniciar
