fix(producao/op): ativar/encerrar por op_id + corrigir op_salvar

- Adiciona POST /producao/op/ativar (ativa OP da FILA e ancora baseline_pcs)
- Adiciona POST /producao/op/encerrar-by-id (encerra OP ativa via op_id)
- Permite editar OP em FILA no helper _update_op_row
- Corrige bug de ordem de definicao do _int e duplicacao de observacoes em op_salvar
