feat(admin): rota temporaria de diagnostico producao_horaria (token)

- Adiciona POST /admin/db-dump-producao-horaria protegido por ADMIN_RESET_TOKEN
- Retorna contagens e agrupamentos para validar persistencia no SQLite do MAIN sem Shell
