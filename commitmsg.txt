feat(multitenant): refugo por cliente_id com compatibilidade legado

- adiciona coluna cliente_id em refugo_horaria (ALTER TABLE se faltar)
- remove indice unico antigo e cria:
  - unique multi-tenant (cliente_id,machine_id,dia_ref,hora_dia)
  - unique legado parcial (machine_id,dia_ref,hora_dia) WHERE cliente_id IS NULL
- suporta machine_id no formato cliente_id::maquina
- dedup automatico antes de criar indices (evita crash no deploy)
