refactor(routes): remover lógica/SQL do Não Programado do machine_routes

- Remove helpers locais de NP (ensure/upsert/process/load) do routes
- POST /machine/update delega persistência de NP para machine_service.processar_nao_programado
- GET /machine/status lê np_por_hora_24 via repos.nao_programado_horaria_repo
- Mantém restante do arquivo fora do escopo
