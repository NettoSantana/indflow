fix(indflow-dev): recuperar ultimo_dia do SQLite no boot

- get_machine agora busca dia_ref mais recente em baseline_diario (ORDER BY updated_at)
- ultimo_dia passa a ser string YYYY-MM-DD (compatível com verificar_reset_diario)
- evita perder referência do dia operacional após deploy/restart
