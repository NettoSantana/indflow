fix(op): nao sobrescrever abertura ao ativar; gravar hora_inicial

- started_at passa a representar ABERTURA_OP (criacao) e nao muda na ativacao
- adiciona coluna hora_inicial em ordens_producao (gravada ao ativar)
- /producao/op/get passa a retornar hora_inicial para o frontend
