fix(historico): Prod do Historico usa producao_por_hora (horas_turno) e remove fallback errado

- Para HOJE, "Prod" do historico passa a ler apenas machine_state.producao_por_hora alinhado a horas_turno
- Remove uso de producao_exibicao_24 no historico (evita ler hora errada)
- Remove lixo no fim do arquivo que podia quebrar import
