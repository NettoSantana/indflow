fix(status): priorizar m.cliente_id na leitura do NP (opcao 1)

- /machine/status passa a resolver cliente_id usando X-API-Key, depois m['cliente_id'], e so entao session (se parecer UUID)
- evita tenant divergente no scoped machine_id e destrava np_por_hora_24 na UI
