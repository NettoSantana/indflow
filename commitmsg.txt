fix(config): persistir unidade_2 e conversão no SQLite (machine_config)

- Adiciona colunas unidade_1, unidade_2, conv_m_por_pcs na tabela machine_config
- Migração segura via PRAGMA table_info + ALTER TABLE
- Upsert agora grava/atualiza esses campos
