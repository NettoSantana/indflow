fix(print): corrige exibicao do tempo de consumo no PDF

- mantém correcao do botao de imprimir
- calcula tempo da bobina com fallback por campos do payload
- usa diferenca entre started_at e ended_at quando necessario
- garante exibicao do TEMPO DE CONSUMO (MIN) na impressao
