feat(producao): adicionar tempo parado oficial no backend (parado_min) e status_ui

- Persiste stopped_since_ms por machine_id em sqlite (machine_stop)
- /machine/update seta/limpa parada conforme status (AUTO vs !=AUTO)
- /machine/status devolve status_ui=PRODUZINDO|PARADA e parado_min (minutos parado)
