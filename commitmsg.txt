feat: endpoint admin hard-reset para limpar SQLite sem shell

- POST /admin/hard-reset (protegido por INDFLOW_ADMIN_TOKEN + header X-Admin-Token)
- limpa tabelas principais (producao/refugo/baseline/config)
