fix(producao_horaria): evitar zerar quando cliente_id muda

- Resolve machine_id real do dia pela tabela producao_horaria (SUM/COUNT) antes de carregar/gravar
- Mantem leitura e persistencia na mesma chave quando existe historico com outro cliente_id
